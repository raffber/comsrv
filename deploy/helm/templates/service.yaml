apiVersion: v1
kind: Service
metadata:
  name: comsrv
  labels:
    app: comsrv
spec:
  type: LoadBalancer
  selector:
    app: comsrv
  ports:
    - port: 5902
      targetPort: 5902
    - port: 5903
      targetPort: 5903
